---
phase: 02-i18n-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/i18n/index.ts
  - src/i18n/locales/en/common.json
  - src/i18n/locales/bg/common.json
  - src/hooks/useLanguageSync.ts
  - src/main.tsx
  - index.html
  - src/index.css
autonomous: true
requirements: [BILN-01, BILN-03, BILN-07]

must_haves:
  truths:
    - "i18next initializes synchronously with bundled EN and BG resources before React renders"
    - "URL :lang param drives i18next language state (not the other way around)"
    - "Bulgarian text renders with proper Cyrillic glyphs via Inter fallback font"
    - "Navigating between /en/ and /bg/ changes the active i18next language"
  artifacts:
    - path: "src/i18n/index.ts"
      provides: "i18next initialization with bundled resources"
      exports: ["default (i18n instance)"]
    - path: "src/i18n/locales/en/common.json"
      provides: "English translation keys for nav and footer"
      contains: "nav.services"
    - path: "src/i18n/locales/bg/common.json"
      provides: "Bulgarian translation keys for nav and footer"
      contains: "nav.services"
    - path: "src/hooks/useLanguageSync.ts"
      provides: "Hook that syncs URL :lang param to i18next language"
      exports: ["useLanguageSync"]
  key_links:
    - from: "src/main.tsx"
      to: "src/i18n/index.ts"
      via: "import './i18n' before App import"
      pattern: "import.*i18n.*\nimport.*App"
    - from: "src/hooks/useLanguageSync.ts"
      to: "react-router-dom useParams"
      via: "reads :lang param and calls i18n.changeLanguage()"
      pattern: "useParams.*lang.*changeLanguage"
    - from: "index.html"
      to: "Google Fonts"
      via: "Inter font loaded alongside Bricolage Grotesque"
      pattern: "family=Inter"
---

<objective>
Install i18next and react-i18next, create the translation file structure with EN and BG nav/footer strings, build the URL-to-i18next sync hook, and add Inter as Cyrillic fallback font.

Purpose: Establish the i18n foundation that the language switcher and translated components will build on. This plan creates the plumbing; the next plan wires it into visible UI.
Output: Working i18n init, translation JSON files, language sync hook, Cyrillic font support.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-i18n-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From src/App.tsx:
```typescript
// Route structure - :lang param is already available
<Route path="/:lang" element={<HomePage />} />
```

From src/main.tsx:
```typescript
// Entry point - i18n import must be added BEFORE App import
import App from './App'
import './index.css'
```

From src/components/ScrollToTop.tsx:
```typescript
// Scrolls to top on pathname change - language switch will trigger this
// Plan 02 must handle this (LanguageSwitcher should not trigger scroll-to-top)
export function ScrollToTop() {
  const { pathname } = useLocation()
  useEffect(() => { window.scrollTo(0, 0) }, [pathname])
}
```

From index.html:
```html
<!-- Current font loading - Inter must be added here -->
<link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&display=swap" rel="stylesheet" />
```

From src/index.css:
```css
/* Current font-family - Inter must be added as fallback */
body { font-family: 'Bricolage Grotesque', sans-serif; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install i18next, create translation files, and init config</name>
  <files>
    package.json
    src/i18n/index.ts
    src/i18n/locales/en/common.json
    src/i18n/locales/bg/common.json
  </files>
  <action>
    1. Run `npm install i18next react-i18next`

    2. Create `src/i18n/locales/en/common.json` with nav and footer translation keys:
       - nav: services, seo, ecommerce, projects, getStarted
       - footer: cta text, getInTouch, sitemap, home, services, projects, servicesColumn, seo, ecommerce, aiAutomation, contact, email, phone, copyright
       Use the exact English text currently hardcoded in Navbar.tsx and Footer.tsx.

    3. Create `src/i18n/locales/bg/common.json` with matching Bulgarian translations:
       - nav.services: "Услуги", nav.seo: "SEO", nav.ecommerce: "Е-Комерс", nav.projects: "Проекти", nav.getStarted: "Започнете"
       - footer.cta: "Нека изградим нещо страхотно заедно. Готови сме да превърнем вашата дигитална визия в реалност."
       - footer.getInTouch: "Свържете се", footer.sitemap: "Карта на сайта", footer.home: "Начало"
       - footer.services: "Услуги", footer.projects: "Проекти", footer.servicesColumn: "Услуги"
       - footer.seo: "SEO", footer.ecommerce: "Е-Комерс", footer.aiAutomation: "AI и Автоматизация"
       - footer.contact: "Контакт", footer.email: "Имейл", footer.phone: "Телефон"
       - footer.copyright: "E&P Systems. Всички права запазени."

    4. Create `src/i18n/index.ts`:
       - Import i18n from 'i18next', initReactI18next from 'react-i18next'
       - Import both JSON files
       - Call i18n.use(initReactI18next).init() with:
         - resources: { en: { common: en }, bg: { common: bg } }
         - lng: 'en', fallbackLng: 'en', defaultNS: 'common'
         - interpolation: { escapeValue: false }
       - Export default i18n
  </action>
  <verify>
    npm run build 2>&1 | tail -5
    # Must compile without errors. Check that i18n/index.ts, both JSON files exist.
  </verify>
  <done>i18next initialized with bundled EN/BG resources, both translation files contain nav and footer keys, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create language sync hook, add Inter font, wire i18n into entry point</name>
  <files>
    src/hooks/useLanguageSync.ts
    src/main.tsx
    index.html
    src/index.css
  </files>
  <action>
    1. Create `src/hooks/useLanguageSync.ts`:
       - Import useEffect from 'react', useParams from 'react-router-dom', useTranslation from 'react-i18next'
       - Define SUPPORTED_LANGS = ['en', 'bg'] as const
       - Export function useLanguageSync() that:
         - Gets { lang } from useParams
         - Gets { i18n } from useTranslation()
         - In useEffect: validates lang against SUPPORTED_LANGS, defaults to 'en', calls i18n.changeLanguage(resolved) if different
         - Also sets document.documentElement.lang = resolved (for accessibility, per research open question 3)
         - Returns the resolved lang string
       - Dependency array: [lang, i18n]

    2. Update `index.html`:
       - Replace the existing Google Fonts link with one that includes Inter:
         `https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&family=Inter:wght@200..900&display=swap`
       - Keep both preconnect links as-is

    3. Update `src/index.css`:
       - Change body font-family from `'Bricolage Grotesque', sans-serif` to `'Bricolage Grotesque', 'Inter', sans-serif`

    4. Update `src/main.tsx`:
       - Add `import './i18n'` BEFORE the `import App from './App'` line (critical: prevents race condition where components render before i18n initializes)
       - Keep everything else the same
  </action>
  <verify>
    npm run build 2>&1 | tail -5
    # Must compile without errors. Verify useLanguageSync.ts exports the hook.
  </verify>
  <done>Language sync hook created, Inter font loaded in index.html, CSS font stack updated, i18n imported in main.tsx before App</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `src/i18n/index.ts` exists and exports configured i18n instance
- `src/i18n/locales/en/common.json` and `bg/common.json` contain matching key structures
- `src/hooks/useLanguageSync.ts` exports useLanguageSync
- `src/main.tsx` imports `./i18n` before `./App`
- `index.html` loads both Bricolage Grotesque and Inter fonts
- `src/index.css` body font-family includes Inter as fallback
</verification>

<success_criteria>
- Build succeeds with i18next and react-i18next installed
- Translation files contain all nav and footer keys in both languages
- Language sync hook reads URL :lang param and syncs to i18next
- Inter font loaded for Cyrillic fallback
- i18n initialized before React renders (import order in main.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/02-i18n-infrastructure/02-01-SUMMARY.md`
</output>
