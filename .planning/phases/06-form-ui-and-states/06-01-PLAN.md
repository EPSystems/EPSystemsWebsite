---
phase: 06-form-ui-and-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contact/ContactModal.tsx
  - src/i18n/locales/en/common.json
  - src/i18n/locales/bg/common.json
autonomous: false
requirements: [FORM-03, FORM-04, CTXT-01]

must_haves:
  truths:
    - "After successful submission, user sees a confirmation message replacing the form fields"
    - "If submission fails, user sees an error message with a retry button that keeps form data intact"
    - "Form shows loading/disabled state while submitting, preventing double-submit"
    - "Each CTA context (SEO, E-Commerce, AI, Software, General) displays a unique heading and description on the form"
  artifacts:
    - path: "src/components/contact/ContactModal.tsx"
      provides: "Context-specific heading and description rendering"
      contains: "contactForm.contexts"
    - path: "src/i18n/locales/en/common.json"
      provides: "Per-context heading and description i18n keys"
      contains: "contactForm.contexts.seo"
    - path: "src/i18n/locales/bg/common.json"
      provides: "Bulgarian per-context heading and description i18n keys"
      contains: "contactForm.contexts.seo"
  key_links:
    - from: "src/components/contact/ContactModal.tsx"
      to: "i18n locales"
      via: "t('contactForm.contexts.{subject}.heading') lookup"
      pattern: "contactForm\\.contexts\\."
    - from: "ContactModalProvider openContactForm"
      to: "ContactModal heading"
      via: "context.subject determines which i18n key is used"
      pattern: "context\\.subject"
---

<objective>
Add per-context form headings/descriptions and verify all submission states work correctly.

Purpose: Each CTA location should display copy that matches user intent (e.g., "Ready to Rank Higher?" on SEO page, not a generic "Get in Touch"). Success, error, and loading states already exist from Phase 5 but need verification against Phase 6 success criteria.

Output: ContactModal with context-specific headings/descriptions per CTA source, verified success/error/loading states.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-form-infrastructure/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 5. Executor should use these directly. -->

From src/components/contact/ContactModalProvider.tsx:

```typescript
export interface FormContext {
  subject: string; // 'general' | 'seo' | 'ecommerce' | 'ai' | 'software'
  source: string; // e.g. 'hero', 'navbar', 'footer', 'seo-cta'
}

export interface ContactModalContextType {
  isOpen: boolean;
  context: FormContext | null;
  openContactForm: (ctx: FormContext) => void;
  closeContactForm: () => void;
}
```

From src/hooks/useContactForm.ts:

```typescript
type FormStatus = "idle" | "submitting" | "success" | "error";
// Returns: { fields, errors, status, updateField, handleSubmit, reset }
```

Current heading logic in ContactModal.tsx (lines 37-42):

```typescript
const heading =
  context?.subject && context.subject !== "general"
    ? t("contactForm.heading.service", {
        service: t("contactForm.services." + context.subject),
      })
    : t("contactForm.heading.general");
```

This produces "Interested in SEO?" - generic interpolation, not context-specific.

Current i18n keys that already exist (DO NOT duplicate):

- contactForm.heading.general = "Get in Touch"
- contactForm.heading.service = "Interested in {{service}}?"
- contactForm.services.seo/ecommerce/ai/software
- contactForm.fields.\* (all labels/placeholders)
- contactForm.validation.\* (all error messages)
- contactForm.submit, contactForm.submitting
- contactForm.success.heading/description/close
- contactForm.error.heading/description/retry

Service page CTA copy already in i18n (use as reference for tone):

- SEO CTA: "Ready to Rank Higher?" / "Get a comprehensive SEO audit..."
- E-Commerce CTA: "Ready to Sell Online?" / "Let's build an online store..."
- AI CTA: "Ready to Work Smarter?" / "Discover how AI and automation..."
- Software CTA: "Have a Project in Mind?" / "Tell us about your idea..."
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-context i18n keys and update ContactModal heading/description logic</name>
  <files>
    src/i18n/locales/en/common.json
    src/i18n/locales/bg/common.json
    src/components/contact/ContactModal.tsx
  </files>
  <action>
1. Add per-context heading and description i18n keys to BOTH locale files. Add a new `contactForm.contexts` object with entries for each CTA context. Use copy that matches the tone of the existing service page CTAs:

EN keys to add under contactForm:

```json
"contexts": {
  "general": {
    "heading": "Get in Touch",
    "description": "Tell us about your project and we'll get back to you within 24 hours."
  },
  "seo": {
    "heading": "Ready to Rank Higher?",
    "description": "Tell us about your SEO goals and we'll show you the opportunities."
  },
  "ecommerce": {
    "heading": "Ready to Sell Online?",
    "description": "Tell us about your store vision and we'll bring it to life."
  },
  "ai": {
    "heading": "Ready to Work Smarter?",
    "description": "Tell us which processes you want to automate."
  },
  "software": {
    "heading": "Have a Project in Mind?",
    "description": "Tell us about your idea and we'll help you build it."
  }
}
```

BG keys (translate to natural Bulgarian, same structure):

```json
"contexts": {
  "general": {
    "heading": "Свържете се с нас",
    "description": "Разкажете ни за вашия проект и ще се свържем до 24 часа."
  },
  "seo": {
    "heading": "Готови ли сте за по-високи класирания?",
    "description": "Разкажете ни за вашите SEO цели и ще ви покажем възможностите."
  },
  "ecommerce": {
    "heading": "Готови ли сте да продавате онлайн?",
    "description": "Разкажете ни за вашата визия за магазин и ще я реализираме."
  },
  "ai": {
    "heading": "Готови ли сте да работите по-умно?",
    "description": "Разкажете ни кои процеси искате да автоматизирате."
  },
  "software": {
    "heading": "Имате проект наум?",
    "description": "Разкажете ни за идеята си и ще ви помогнем да я реализирате."
  }
}
```

Keep the existing `contactForm.heading` and `contactForm.services` keys - they are not removed (other code may reference them). Add `contexts` as a NEW sibling key.

2. Update ContactModal.tsx heading logic (lines 37-42). Replace the current heading derivation with:

```typescript
const subject = context?.subject || "general";
const contextKey = subject === "general" ? "general" : subject;
const heading = t(`contactForm.contexts.${contextKey}.heading`);
const description = t(`contactForm.contexts.${contextKey}.description`);
```

3. Add the description text below the heading in the form view. After the existing `<h2>` heading element (line 97), add:

```tsx
<p className="text-gray-600 mb-6">{description}</p>
```

This replaces the bare heading with heading + description for every context.

Do NOT modify the success state view, error state view, or loading/disabled state - they are already correct from Phase 5.
</action>
<verify>
<automated>cd D:/Claude/EPSystems && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done> - contactForm.contexts.{general,seo,ecommerce,ai,software}.{heading,description} keys exist in both EN and BG locale files - ContactModal renders context-specific heading and description based on context.subject - TypeScript compiles without errors
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify form states and context-specific headings</name>
  <files>src/components/contact/ContactModal.tsx</files>
  <action>
Present the dev server to the user for visual verification of all Phase 6 success criteria. Start dev server with `npm run dev` if not already running. Guide user through the verification steps below.
  </action>
  <verify>User confirms all states and headings render correctly</verify>
  <done>All four Phase 6 success criteria confirmed by user</done>
  <what-built>
    Context-specific form headings and descriptions per CTA source, plus existing success/error/loading states from Phase 5.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the site
    2. Test context-specific headings:
       - Open browser console, run: `document.querySelector('[data-testid]') || 'no testid'` (just to confirm app loads)
       - If any CTA button is wired to openContactForm already, click it and verify the heading matches the context
       - Otherwise, test via React DevTools: find ContactModalProvider, manually trigger openContactForm with different contexts:
         - `{subject: 'seo', source: 'test'}` should show "Ready to Rank Higher?"
         - `{subject: 'general', source: 'test'}` should show "Get in Touch"
         - `{subject: 'ai', source: 'test'}` should show "Ready to Work Smarter?"
       - Verify description text appears below heading
    3. Test form states (these should already work from Phase 5):
       - Submit with empty fields: inline validation errors appear
       - Fill valid data, submit: button shows "Sending..." and is disabled
       - On success: form is replaced with "Message Sent!" confirmation
       - On network error: error banner appears above submit with "Try Again" link, form data is preserved
    4. Switch language to BG and verify headings are in Bulgarian
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `contactForm.contexts` key exists in both en/common.json and bg/common.json with 5 context entries
- ContactModal.tsx uses `contactForm.contexts.{subject}` pattern instead of generic interpolation
- Success, error, and loading states render correctly (unchanged from Phase 5)
</verification>

<success_criteria>

1. Each CTA context (general, seo, ecommerce, ai, software) shows a unique, intent-matching heading and description in both EN and BG
2. Success state replaces form with confirmation message and close button
3. Error state shows inline banner with retry, preserving form data
4. Loading state disables submit button and shows "Sending..." text
5. All form text is bilingual (EN/BG)
   </success_criteria>

<output>
After completion, create `.planning/phases/06-form-ui-and-states/06-01-SUMMARY.md`
</output>
