---
phase: 01-router-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/utils/scroll.ts
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Services.tsx
  - src/components/sections/CaseStudies.tsx
autonomous: false
requirements:
  - ROUT-02
  - ROUT-05

must_haves:
  truths:
    - "Clicking a navbar anchor link (Services, SEO, E-Commerce, Projects) smooth-scrolls to that section"
    - "Clicking Get Started in navbar smooth-scrolls to the contact/CTA section"
    - "No placeholder links (href='#') exist anywhere on the site"
    - "Service card 'Learn more' links scroll to the corresponding ServiceDetail section"
    - "All external links (email, phone) open correctly"
    - "Footer sitemap links navigate properly within the router"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Router-aware navigation with smooth scroll"
      contains: "useNavigate\\|scrollIntoView\\|Link"
    - path: "src/components/layout/Footer.tsx"
      provides: "Cleaned up links with no placeholders"
    - path: "src/components/sections/Hero.tsx"
      provides: "Router-aware CTA links"
    - path: "src/components/sections/Services.tsx"
      provides: "Service card links that scroll to detail sections"
    - path: "src/components/sections/CaseStudies.tsx"
      provides: "Router-aware contact link"
  key_links:
    - from: "Navbar anchor links"
      to: "Section IDs on HomePage"
      via: "scrollIntoView with smooth behavior"
      pattern: "scrollIntoView.*smooth"
    - from: "Footer sitemap links"
      to: "Router routes"
      via: "React Router Link or scrollIntoView"
      pattern: "Link to=\\|scrollIntoView"
---

<objective>
Migrate all hash-based links to router-aware navigation and fix every broken/placeholder link.

Purpose: After Plan 01 established the router, existing hash links (href="#services") no longer work correctly with BrowserRouter. This plan converts them to programmatic smooth-scroll and removes all dead links.
Output: Every link on the site works correctly -- anchor links smooth-scroll, navigation links use the router, placeholder links are removed.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-router-foundation/01-CONTEXT.md
@.planning/phases/01-router-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Section IDs used as anchor targets across the homepage -->
<!-- These IDs exist on section elements and must be preserved -->

Section IDs (from HomePage/components):
- id="services" (Services.tsx - services grid)
- id="seo" (ServiceDetail section)
- id="ecommerce" (ServiceDetail section)
- id="ai" (ServiceDetail section)
- id="case-studies" (CaseStudies.tsx)
- id="contact" (CTA.tsx)
- id="footer" (Footer.tsx)

Current hash links that need migration:

Navbar.tsx (desktop + mobile):
- href="#services" (Services)
- href="#seo" (SEO)
- href="#ecommerce" (E-Commerce)
- href="#case-studies" (Projects)
- href="#contact" (Get Started button)

Footer.tsx:
- href="#" (Home link - PLACEHOLDER, must fix)
- href="#services" (Services)
- href="#case-studies" (Projects)
- href="#seo", href="#ecommerce", href="#ai" (service links)
- href="#" (Privacy Policy - PLACEHOLDER, must remove or disable)
- href="#" (Terms of Use - PLACEHOLDER, must remove or disable)
- ArrowUpRight button next to "Get in touch" (no href, does nothing)

Hero.tsx:
- href="#services" (Browse Services link)

Services.tsx:
- href={`#${service.id}`} (Learn more on each service card)

CaseStudies.tsx:
- href="#contact" (contact link in case study cards)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all hash links to router-aware smooth-scroll and remove placeholder links</name>
  <files>
    src/utils/scroll.ts
    src/components/layout/Navbar.tsx
    src/components/layout/Footer.tsx
    src/components/sections/Hero.tsx
    src/components/sections/Services.tsx
    src/components/sections/CaseStudies.tsx
  </files>
  <action>
Create a smooth-scroll helper approach. Since all anchor targets are on the homepage (single page for now), use a simple pattern:

For same-page anchor links, use an onClick handler that calls a scroll utility.

Create `src/utils/scroll.ts`:
- Export `scrollToSection(sectionId: string): void` that calls `document.getElementById(sectionId)?.scrollIntoView({ behavior: 'smooth' })`

**Navbar.tsx changes:**
- Import `useParams` from react-router-dom (for lang-aware logo link)
- Replace all `href="#services"` etc. with `<button>` or `<a href="#services" onClick={handler}>` pattern
- Use the scrollToSection utility for: Services, SEO, E-Commerce, Projects, Get Started
- Keep the same visual styles and mobile menu behavior
- Mobile menu: close menu on click (existing behavior) AND scroll to section
- The E&P Systems logo/brand should link to `/${lang}/` using react-router-dom `Link` (or just `/en/` for now)

**Footer.tsx changes:**
- Sitemap "Home" link: change from `href="#"` to react-router-dom `<Link to="/en/">` (or use `useParams` to get current lang)
- Sitemap "Services" and "Projects": use scrollToSection (same-page anchors)
- Services column (SEO, E-Commerce, AI & Automation): use scrollToSection
- **Remove** "Privacy Policy" and "Terms of Use" links entirely -- per user decision: "better no link than a broken one". Remove the entire Legal column from the footer grid. Adjust grid to `grid-cols-2 sm:grid-cols-3` instead of `sm:grid-cols-4`.
- The ArrowUpRight button next to "Get in touch": make it scroll to top of page (`window.scrollTo({ top: 0, behavior: 'smooth' })`)
- Email and Phone links: keep as-is (mailto: and tel: are fine, NOT placeholders)

**Hero.tsx changes:**
- The `href="#services"` "Browse Services" link: use scrollToSection
- The `mailto:` link: keep as-is

**Services.tsx changes:**
- Each service card's `href={`#${service.id}`}` "Learn more" link: use scrollToSection
- This scrolls to the ServiceDetail section for that service (ids: seo, ecommerce, ai)
- For services WITHOUT a detail section (websites, landing, support), the link currently points to a nonexistent anchor. These service cards should NOT have a "Learn more" link. Conditionally render the link only if `service.detailHeadline` exists (indicates a detail section exists).

**CaseStudies.tsx changes:**
- The `href="#contact"` links: use scrollToSection to scroll to the CTA section

IMPORTANT: Do NOT touch ServiceDetail.tsx or CTA.tsx -- their section IDs are anchor targets, not sources of links that need fixing. CTA.tsx mailto link is fine as-is.
  </action>
  <verify>
    <automated>cd D:/Claude/EPSystems && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build succeeds with no errors
    - Zero instances of `href="#"` (bare hash) remain in any component
    - All navbar links smooth-scroll to their target sections
    - Service cards with detail sections scroll to them; cards without detail sections have no dead "Learn more" link
    - Footer has no Legal column, no placeholder links
    - Footer Home link navigates via router
    - Footer ArrowUpRight button scrolls to top
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all navigation works end-to-end</name>
  <files>none</files>
  <action>
User manually verifies the complete router foundation across both plans.
Start the dev server with `npm run dev` and walk through all navigation paths.
  </action>
  <verify>User confirms all 10 verification steps pass</verify>
  <done>User types "approved" after confirming all navigation works</done>
  <what-built>Complete router foundation with language-prefixed URLs and migrated navigation links. All hash-based links converted to smooth-scroll, placeholder links removed, 404 page added.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/
    2. Confirm you are redirected to /en/ in the URL bar
    3. Click each navbar link (Services, SEO, E-Commerce, Projects) -- each should smooth-scroll to its section
    4. Click "Get Started" in navbar -- should smooth-scroll to the CTA section
    5. Scroll to the Services grid -- click "Learn more" on SEO, E-Commerce, AI cards -- each scrolls to its detail section
    6. Confirm "Custom Websites", "Landing Pages", "Maintenance" cards do NOT have "Learn more" links
    7. Scroll to footer:
       - "Home" link navigates properly
       - "Services", "Projects" links scroll to sections
       - No "Privacy Policy" or "Terms of Use" links exist
       - ArrowUpRight button scrolls to top of page
    8. Manually visit /bg/ -- homepage renders identically
    9. Visit /some/fake/path -- 404 page appears with link back to homepage
    10. Click the 404 link back -- returns to homepage
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `grep -r 'href="#"' src/` returns zero results (no bare hash placeholders)
3. All smooth-scroll targets have matching section IDs on the page
4. Router navigation works for / -> /en/ redirect
5. 404 catches unknown routes
</verification>

<success_criteria>
- All anchor links smooth-scroll to correct sections
- No placeholder or broken links remain anywhere
- Footer cleaned up (Legal column removed)
- Service cards conditionally show "Learn more" only when detail section exists
- Build passes, dev server works correctly
- User has verified all navigation manually
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-foundation/01-02-SUMMARY.md`
</output>
