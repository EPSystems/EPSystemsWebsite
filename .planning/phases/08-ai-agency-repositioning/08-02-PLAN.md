---
phase: 08-ai-agency-repositioning
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-ai-agency-repositioning/08-FINDINGS.md
autonomous: true
requirements: [REPO-02, REPO-06, REPO-07, REPO-08, REPO-09]
must_haves:
  truths:
    - "Homepage at /bg/ shows the AI-agency positioning sections in Bulgarian; /en/ shows the same content in English"
    - "Switching locale actually changes rendered text (bg ≠ en) and document.documentElement.lang"
    - "Navbar exposes a Services dropdown and an Industries dropdown that open and list items"
    - "The mobile menu opens and exposes the same navigation"
    - "Pricing, About, Contact, and Privacy-Policy pages render bilingually with no blank page / error boundary"
  artifacts:
    - path: src/pages/HomePage.tsx
      provides: "Homepage section composition (Hero, Services, Results, WhyUs, Testimonials, TrustedBy, Team, CTA)"
      contains: "Hero"
    - path: src/components/layout/Navbar.tsx
      provides: "Services + Industries dropdowns (desktop) and mobile accordion menu"
      contains: "desktopDropdown"
    - path: src/pages/Pricing.tsx
      provides: "Pricing page"
    - path: src/pages/About.tsx
      provides: "About/Team page"
    - path: src/pages/Contact.tsx
      provides: "Contact page"
    - path: src/pages/PrivacyPolicy.tsx
      provides: "Privacy Policy page"
  key_links:
    - from: src/pages/HomePage.tsx
      to: i18n locale resources
      via: "useLanguageSync resolves lang from URL and i18n.changeLanguage"
      pattern: "useLanguageSync"
    - from: src/components/layout/Navbar.tsx
      to: "/:lang/services/:slug and /:lang/industries/:slug"
      via: "Link to dropdown items"
      pattern: "services/\\$\\{"
---

<objective>
Browser-verify the core pages of the shipped AI-agency site in BOTH locales: the homepage positioning (REPO-02), the navbar dropdowns + mobile menu (REPO-06), and the standalone Pricing (REPO-07), About/Team (REPO-08), Contact, and Privacy-Policy (REPO-09) pages. Prove each route renders real localized content (no blank page, no error boundary) and that the language genuinely switches between bg and en.

This is a verification plan — pages already exist. Do not rebuild. Log defects to 08-FINDINGS.md per the defect policy.

Purpose: Confirm the user-facing core of the repositioning works end-to-end before declaring REPO-02/06/07/08/09 done.
Output: Per-route PASS/FAIL evidence appended to 08-FINDINGS.md, with snapshots for each verified route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/App.tsx
@src/pages/HomePage.tsx
@src/components/layout/Navbar.tsx

<interfaces>
<!-- Server + route facts the executor needs. Use Playwright MCP for browser steps. -->

Preview server: run `pnpm preview` (serves built dist/ at http://localhost:4173). Plan 08-01 already produced a green build, so `dist/` exists. If preview refuses because dist is stale, run `pnpm build` first. Alternatively `pnpm dev` serves at http://localhost:5173 — use whichever starts cleanly; record the base URL used.

Homepage section components (rendered by src/pages/HomePage.tsx, in order): Hero, Marquee, Services (sections/Services.tsx), Results, WhyUs, Testimonials, TrustedBy, Team, CTA, Footer. Note REPO-02 lists "Why Us, Results, Testimonials, TrustedBy, Team, CTA" — all present.

Navbar (src/components/layout/Navbar.tsx):
- Desktop: a "Services" button (text from `nav.services`) with ChevronDown that toggles a dropdown listing 5 service Links (to `/:lang/services/{slug}`) plus an "all services" Link; an "Industries" button (text from `nav.industries`) toggling a dropdown of 3 industry Links. Dropdowns appear only at the `xl` breakpoint (>=1280px wide). Desktop dropdown is click-to-open (`onClick` toggles `desktopDropdown`), NOT hover.
- Mobile (below xl): a hamburger button with `aria-label="Toggle menu"` toggles a menu containing Services/Industries accordions and Blog/About/Contact links.
- To exercise desktop dropdowns, set the Playwright viewport width >= 1280. To exercise the mobile menu, set width <= 1024 and click the element with `aria-label="Toggle menu"`.

Locale-switch assertion: useLanguageSync sets `document.documentElement.lang` to the URL lang. The bg homepage and en homepage must render DIFFERENT visible heading text (Bulgarian vs English). Capture a stable heading string in bg, then in en, and assert they differ.

Standalone pages: Pricing (`/:lang/pricing`), About (`/:lang/about`), Contact (`/:lang/contact`), PrivacyPolicy (`/:lang/privacy-policy`). Each renders Navbar + content + Footer. A correctly rendered page has a non-empty `<h1>` (or prominent heading) and is NOT the NotFoundPage. NotFoundPage is the catch-all `*` route — if a known route renders NotFound content, that is a routing defect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify homepage positioning + locale switch (REPO-02)</name>
  <read_first>src/pages/HomePage.tsx, src/App.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    Start the server (`pnpm preview`, base http://localhost:4173 — or `pnpm dev` at :5173 if preview fails; record which). Then, using Playwright MCP at a desktop viewport (width 1440, height 900):

    1. Navigate to `http://localhost:4173/` and assert the URL after load is `/bg/` (root redirect to Bulgarian — corroborates REPO-01 in-browser).
    2. On `/bg/`: take a snapshot. Assert the page is NOT blank and shows NO error boundary. Assert the homepage sections render — confirm presence of: a Hero heading (top `<h1>`), a services section, a results/metrics section, a team section, and a footer. Capture the Hero `<h1>` text (Bulgarian) as BG_HERO.
    3. Navigate to `http://localhost:4173/en/`. Take a snapshot. Assert NOT blank / NO error boundary. Capture the Hero `<h1>` text (English) as EN_HERO. Assert `document.documentElement.lang === 'en'` (evaluate in page) and that `BG_HERO !== EN_HERO` (the same hero rendered in two different languages).
    4. Append to 08-FINDINGS.md a `## Core pages (Plan 08-02)` section with a `### REPO-02 Homepage` subsection: base URL used, `/` redirect result, BG_HERO, EN_HERO, lang-switch PASS/FAIL, and which sections were found.

    Defect policy: a blank homepage or error boundary on `/bg/` or `/en/` is a substantial defect — log it with the route and flag for a gap plan. A single missing translation key showing a raw key string (e.g. `meta.title`) is a trivial defect — note it as trivial; only fix inline if the fix is an obvious key addition in `src/i18n/locales/{lang}/common.json`, otherwise log it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');if(!/REPO-02 Homepage/.test(t)){console.error('REPO-02 finding not recorded');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - After navigating to `/`, the browser URL is `http://localhost:4173/bg/` (or the dev-server equivalent with `/bg/`).
    - `/bg/` snapshot shows a non-empty Hero `<h1>`, a services section, a results section, a team section, and a footer; no error-boundary text.
    - `/en/` snapshot is non-empty; `document.documentElement.lang === 'en'`; BG_HERO !== EN_HERO.
    - `08-FINDINGS.md` contains a `### REPO-02 Homepage` subsection recording BG_HERO, EN_HERO, and the lang-switch result.
  </acceptance_criteria>
  <done>The homepage renders the AI-agency positioning sections in Bulgarian at /bg/ and in English at /en/, the root redirect lands on /bg/, and the language genuinely switches. Evidence and any defects are logged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify navbar dropdowns + mobile menu (REPO-06)</name>
  <read_first>src/components/layout/Navbar.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    With the server still running, use Playwright MCP.

    Desktop dropdowns (set viewport width 1440 x 900 so the xl nav is visible):
    1. On `/bg/`: click the "Services" nav button (the button containing the `nav.services` label with a ChevronDown). Snapshot and assert the dropdown opened — it should reveal 5 service links whose hrefs are `/bg/services/ai-websites`, `/bg/services/ai-automation`, `/bg/services/ai-agents`, `/bg/services/ai-seo`, `/bg/services/ai-ecommerce`, plus an "all services" link to `/bg/services`.
    2. Click the "Industries" nav button. Snapshot and assert it reveals 3 links: `/bg/industries/insurance`, `/bg/industries/ecommerce`, `/bg/industries/fitness`.
    3. Repeat steps 1-2 on `/en/` (hrefs now `/en/...`) to confirm bilingual exposure. Capture the Services button label text in bg vs en and assert they differ (label is localized).

    Mobile menu (set viewport width 390 x 844, iPhone-ish, below xl):
    4. On `/bg/`: the desktop nav links are hidden; click the element with `aria-label="Toggle menu"`. Snapshot and assert a menu panel appeared containing the Services accordion button and the Blog/About/Contact links. Click the Services accordion and assert it expands the 5 service links. Then click a service link (e.g. ai-agents) and assert navigation to `/bg/services/ai-agents`.

    Append a `### REPO-06 Navbar` subsection to 08-FINDINGS.md with: desktop Services dropdown (5 links) PASS/FAIL, desktop Industries dropdown (3 links) PASS/FAIL, bilingual label difference, mobile menu open PASS/FAIL, mobile accordion+navigate PASS/FAIL.

    Defect policy: if a dropdown does not open, or a listed item links to a wrong/404 slug, that is a defect. A wrong slug in the Navbar SERVICES/INDUSTRIES arrays is a trivial defect — fix inline in `src/components/layout/Navbar.tsx` to match the documented slugs. A dropdown that never opens (broken state handler) is substantial — log and flag.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');if(!/REPO-06 Navbar/.test(t)){console.error('REPO-06 finding not recorded');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Desktop Services dropdown opens and shows links to all 5 documented service slugs plus the all-services link.
    - Desktop Industries dropdown opens and shows links to insurance, ecommerce, fitness.
    - The Services button label differs between bg and en (localized).
    - At a mobile viewport, clicking `aria-label="Toggle menu"` opens a menu; the Services accordion expands and a service link navigates to `/bg/services/ai-agents`.
    - `08-FINDINGS.md` contains a `### REPO-06 Navbar` subsection with all five PASS/FAIL lines.
  </acceptance_criteria>
  <done>The navbar exposes working Services and Industries dropdowns in both languages, and the mobile menu opens and navigates correctly. Evidence and any defects are logged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Pricing, About, Contact, Privacy pages bilingually (REPO-07, REPO-08, REPO-09)</name>
  <read_first>src/pages/Pricing.tsx, src/pages/About.tsx, src/pages/Contact.tsx, src/pages/PrivacyPolicy.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    With the server still running, use Playwright MCP at desktop viewport (1440 x 900). For EACH of these 4 routes, navigate in BOTH locales (8 navigations total) and verify each renders real content:
    - Pricing: `/bg/pricing` and `/en/pricing` (REPO-07)
    - About/Team: `/bg/about` and `/en/about` (REPO-08)
    - Contact: `/bg/contact` and `/en/contact` (REPO-09)
    - Privacy Policy: `/bg/privacy-policy` and `/en/privacy-policy` (REPO-09)

    For each navigation:
    1. Take a snapshot.
    2. Assert the page is NOT blank and shows NO error boundary.
    3. Assert it is NOT the NotFoundPage (the catch-all). Confirm a real, non-empty prominent heading is present.
    4. For the bg vs en pair of each page, capture the main heading in each locale and assert they differ (content is localized) — UNLESS the page legitimately has a brand-only heading; if a heading is identical across locales, capture a paragraph string instead and assert that differs. Record what you compared.

    Append a `### Standalone pages (REPO-07/08/09)` subsection to 08-FINDINGS.md with a small table: route | locale | rendered? | heading captured | localized (bg≠en)?

    Defect policy: a route rendering NotFoundPage content = routing defect (substantial — log + flag). A page that renders but shows raw translation keys = trivial; fix inline only if it is an obvious missing key in `src/i18n/locales/{lang}/common.json`, otherwise log as trivial. A fully blank/error-boundary page = substantial — log + flag.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');const need=['/bg/pricing','/en/pricing','/bg/about','/en/about','/bg/contact','/en/contact','/bg/privacy-policy','/en/privacy-policy'];const miss=need.filter(r=>!t.includes(r));if(miss.length){console.error('Routes not recorded in findings:',miss.join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 navigations (4 pages x 2 locales) render non-blank, non-error-boundary, non-NotFound pages with a real heading.
    - For each page, the bg and en versions render localized content (captured heading or paragraph differs, with what-was-compared recorded).
    - `08-FINDINGS.md` `### Standalone pages` subsection lists all 8 route+locale combinations with rendered? and localized? results.
  </acceptance_criteria>
  <done>Pricing, About, Contact, and Privacy-Policy all load and render bilingually with real localized content and no NotFound/blank/error states. Evidence and any defects are logged.</done>
</task>

</tasks>

<verification>
- Root `/` redirects to `/bg/` in the browser; homepage renders all REPO-02 sections in bg and en with genuine locale switching.
- Navbar Services (5 links) + Industries (3 links) dropdowns open in both locales; mobile menu opens and navigates.
- Pricing, About, Contact, Privacy-Policy each render bilingually (8/8 navigations pass).
- 08-FINDINGS.md contains the `## Core pages (Plan 08-02)` section with REPO-02, REPO-06, and standalone-pages subsections.
- Any defect found is classified (trivial → fixed inline; substantial → logged + flagged for gap plan) and recorded.
</verification>

<success_criteria>
- REPO-02, REPO-06, REPO-07, REPO-08, REPO-09 verified end-to-end in the browser across bg and en.
- No unlogged defects; substantial defects (if any) flagged for a follow-up gap plan rather than rebuilt.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-agency-repositioning/08-02-SUMMARY.md`.
</output>
