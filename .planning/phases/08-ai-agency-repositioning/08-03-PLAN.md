---
phase: 08-ai-agency-repositioning
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-ai-agency-repositioning/08-FINDINGS.md
autonomous: true
requirements: [REPO-03, REPO-04, REPO-05]
must_haves:
  truths:
    - "All 5 AI service pages resolve at /:lang/services/{slug} and render their section stack in both locales"
    - "The services hub at /:lang/services lists and links to all 5 AI pillars"
    - "All 3 industry pages resolve at /:lang/industries/{slug} and render bilingually"
    - "Service and industry pages render real localized content (bg ≠ en), no blank page / error boundary"
  artifacts:
    - path: src/pages/ServicePage.tsx
      provides: "Service page composing Hero/Features/Personas/Process/CaseStudy/Pricing/FAQ/Related/CTA per slug"
      contains: "VALID_SLUGS"
    - path: src/pages/Services.tsx
      provides: "Services hub linking to all 5 service slugs"
      contains: "ai-websites"
    - path: src/pages/Industry.tsx
      provides: "Industry page for insurance/ecommerce/fitness"
      contains: "VALID_SLUGS"
  key_links:
    - from: src/pages/Services.tsx
      to: "/:lang/services/:slug"
      via: "Link per service card"
      pattern: "services/\\$\\{"
    - from: src/pages/ServicePage.tsx
      to: i18n servicePages.{slug} keys
      via: "usePageMeta + section components keyed by slug"
      pattern: "servicePages\\.\\$\\{"
---

<objective>
Browser-verify the service and industry surfaces of the shipped AI-agency site in BOTH locales: all five AI service pillar pages (REPO-03), the services hub that links to them (REPO-04), and the three industry verticals (REPO-05). Prove each route renders its full section stack with real localized content (no blank page, no error boundary, no NotFound), and that the hub actually links to every pillar.

This is a verification plan — pages already exist. Do not rebuild. Log defects to 08-FINDINGS.md per the defect policy. This plan runs in parallel with 08-02 (Wave 2); both only depend on the green build from 08-01 and touch disjoint routes.

Purpose: Confirm the core agency offering (5 pillars + 3 verticals + hub) works end-to-end before declaring REPO-03/04/05 done.
Output: Per-route PASS/FAIL evidence appended to 08-FINDINGS.md, with snapshots for each verified route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/pages/ServicePage.tsx
@src/pages/Services.tsx
@src/pages/Industry.tsx

<interfaces>
<!-- Server + route facts the executor needs. Use Playwright MCP for browser steps. -->

Preview server: `pnpm preview` (built dist/ at http://localhost:4173). Plan 08-01 produced a green build. If preview fails on stale dist, run `pnpm build` first. `pnpm dev` (:5173) is an acceptable fallback — record the base URL used. If 08-02 already started a server, reuse it.

Service slugs (src/pages/ServicePage.tsx VALID_SLUGS): ai-websites, ai-automation, ai-agents, ai-seo, ai-ecommerce.
ServicePage renders this section stack per slug (src/pages/ServicePage.tsx), all keyed off `servicePages.{slug}.*` i18n keys: ServiceHero, ServiceFeatures, ServicePersonas, ServiceProcess, ServiceCaseStudy, ServicePricing, ServiceFAQ, ServiceRelated, ServiceCTA. The page also injects a per-page `<script type="application/ld+json" data-service-schema="{slug}">` (Service schema) — its presence corroborates the slug rendered, but the JSON-LD itself is verified in Phase 10 (SEO-09), not here.
Invalid service slugs redirect: unknown slug → `/:lang/services`; legacy slugs (seo, software, ecommerce, ai, landing, maintenance) → a canonical ai-* slug. Do NOT treat these redirects as defects.

Services hub (src/pages/Services.tsx): renders a grid of 5 cards, each a `Link` to `/:lang/services/{href}` where href ∈ {ai-websites, ai-automation, ai-agents, ai-seo, ai-ecommerce}.

Industry slugs (src/pages/Industry.tsx VALID_SLUGS): insurance, ecommerce, fitness. Industry renders Hero (badge/heading/subheading), Problems (4 cards), Solutions (4 cards), Proof, CTA — all keyed off `industries.{slug}.*`. Unknown industry slug → redirect to `/:lang/`.

Render-OK definition: page is non-blank, shows no error-boundary text, is not the NotFoundPage, and has a non-empty top heading. Localized definition: the captured heading (or a paragraph) differs between bg and en.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 5 AI service pages render bilingually (REPO-03)</name>
  <read_first>src/pages/ServicePage.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    Start/reuse the server (record base URL). Using Playwright MCP at desktop viewport (1440 x 900), for EACH of the 5 service slugs (ai-websites, ai-automation, ai-agents, ai-seo, ai-ecommerce), navigate in BOTH locales (10 navigations):
    - `/bg/services/{slug}` and `/en/services/{slug}`

    For each navigation:
    1. Take a snapshot. Assert render-OK (non-blank, no error boundary, not NotFound, non-empty top heading).
    2. Assert the section stack is present — confirm the page contains, beyond the hero: a features section, a process/steps section, a pricing section, and a FAQ section (these correspond to ServiceFeatures, ServiceProcess, ServicePricing, ServiceFAQ). It is sufficient to confirm multiple distinct content sections render (the page is long, not a single hero). Also assert a related-services section and a CTA near the bottom.
    3. For the bg vs en pair, capture the hero heading in each locale and assert bg ≠ en (localized).
    4. Confirm the page did NOT redirect to `/:lang/services` (i.e. the slug was treated as valid).

    Append to 08-FINDINGS.md a `## Services & Industries (Plan 08-03)` section with a `### REPO-03 Service pages` table: slug | locale | render-OK | sections found (hero/features/process/pricing/faq/related/cta) | localized?

    Defect policy: a service page that redirects away to the hub (slug not recognized) when it should be valid = substantial defect (log + flag). A blank/error page = substantial. Raw translation keys showing = trivial (fix inline only if obviously a missing key in `src/i18n/locales/{lang}/common.json`, else log).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');const slugs=['ai-websites','ai-automation','ai-agents','ai-seo','ai-ecommerce'];const miss=slugs.filter(s=>!t.includes('/bg/services/'+s)||!t.includes('/en/services/'+s));if(!/REPO-03 Service pages/.test(t)){console.error('REPO-03 section missing');process.exit(1)}if(miss.length){console.error('Service slugs not recorded in both locales:',miss.join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 service slugs render-OK in both bg and en (10/10 navigations); none redirect to the hub.
    - Each service page shows hero + features + process + pricing + FAQ + related + CTA sections.
    - Each slug's bg and en hero heading differ (localized).
    - `08-FINDINGS.md` `### REPO-03 Service pages` table lists all 10 slug+locale combinations with results.
  </acceptance_criteria>
  <done>All five AI service pages resolve at /:lang/services/{slug}, render their full section stack, and are bilingual. Evidence and any defects are logged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify services hub links to all 5 pillars (REPO-04)</name>
  <read_first>src/pages/Services.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    With the server running, use Playwright MCP at desktop viewport (1440 x 900):
    1. Navigate to `/bg/services`. Take a snapshot. Assert render-OK (non-blank, no error boundary, not NotFound).
    2. Assert the hub contains links (anchors) to all 5 service slugs: hrefs `/bg/services/ai-websites`, `/bg/services/ai-automation`, `/bg/services/ai-agents`, `/bg/services/ai-seo`, `/bg/services/ai-ecommerce`. Count the service-card links — must be 5.
    3. Click the ai-automation card link and assert navigation to `/bg/services/ai-automation` and render-OK there; then go back.
    4. Navigate to `/en/services`. Assert render-OK and that the 5 links now point to `/en/services/{slug}`. Capture the hub hero heading in bg vs en and assert they differ (localized).

    Append a `### REPO-04 Services hub` subsection to 08-FINDINGS.md: bg hub render-OK, 5 links present (list them), click-through to ai-automation result, en hub render-OK + 5 links, localized?.

    Defect policy: hub missing a pillar link or a card linking to a wrong slug = trivial (fix inline in `src/pages/Services.tsx` `services` array to match documented slugs). Hub blank/error = substantial (log + flag).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');if(!/REPO-04 Services hub/.test(t)){console.error('REPO-04 section missing');process.exit(1)}const need=['/bg/services/ai-websites','/bg/services/ai-automation','/bg/services/ai-agents','/bg/services/ai-seo','/bg/services/ai-ecommerce'];const miss=need.filter(r=>!t.includes(r));if(miss.length){console.error('Hub links not recorded:',miss.join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `/bg/services` renders-OK and contains exactly 5 service-card links to the 5 documented slugs.
    - Clicking the ai-automation card navigates to `/bg/services/ai-automation` (render-OK).
    - `/en/services` renders-OK with the 5 links pointing to `/en/services/{slug}`; hub heading is localized (bg ≠ en).
    - `08-FINDINGS.md` `### REPO-04 Services hub` subsection lists all 5 hub links and the click-through result.
  </acceptance_criteria>
  <done>The services hub at /:lang/services lists and links to all five AI pillars, navigates correctly, and renders bilingually. Evidence and any defects are logged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all 3 industry pages render bilingually (REPO-05)</name>
  <read_first>src/pages/Industry.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    With the server running, use Playwright MCP at desktop viewport (1440 x 900). For EACH of the 3 industry slugs (insurance, ecommerce, fitness), navigate in BOTH locales (6 navigations):
    - `/bg/industries/{slug}` and `/en/industries/{slug}`

    For each navigation:
    1. Take a snapshot. Assert render-OK (non-blank, no error boundary, not NotFound, non-empty top heading).
    2. Confirm the page did NOT redirect to `/:lang/` (i.e. the slug was treated as valid).
    3. Assert the page renders its structure: a hero heading, a Problems section (badge + heading + cards), a Solutions section, a Proof block, and a CTA. It is sufficient to confirm the hero heading plus at least the Problems and Solutions headings render.
    4. For the bg vs en pair, capture the hero heading in each locale and assert bg ≠ en (localized).

    Append a `### REPO-05 Industry pages` table to 08-FINDINGS.md: slug | locale | render-OK | sections found (hero/problems/solutions/proof/cta) | localized?

    Defect policy: an industry page that redirects to `/:lang/` when the slug should be valid = substantial defect (log + flag). Blank/error = substantial. Raw translation keys = trivial (fix inline only if obviously a missing key in `src/i18n/locales/{lang}/common.json`, else log).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md','utf8');if(!/REPO-05 Industry pages/.test(t)){console.error('REPO-05 section missing');process.exit(1)}const slugs=['insurance','ecommerce','fitness'];const miss=slugs.filter(s=>!t.includes('/bg/industries/'+s)||!t.includes('/en/industries/'+s));if(miss.length){console.error('Industry slugs not recorded in both locales:',miss.join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 industry slugs render-OK in both bg and en (6/6 navigations); none redirect to `/:lang/`.
    - Each industry page shows hero + Problems + Solutions (+ Proof + CTA) sections.
    - Each slug's bg and en hero heading differ (localized).
    - `08-FINDINGS.md` `### REPO-05 Industry pages` table lists all 6 slug+locale combinations with results.
  </acceptance_criteria>
  <done>All three industry verticals resolve at /:lang/industries/{slug}, render their structure, and are bilingual. Evidence and any defects are logged.</done>
</task>

</tasks>

<verification>
- All 5 service pages render-OK with full section stacks in bg and en (10/10); none wrongly redirect.
- Services hub links to all 5 pillars in both locales and click-through works.
- All 3 industry pages render-OK bilingually (6/6); none wrongly redirect.
- 08-FINDINGS.md contains the `## Services & Industries (Plan 08-03)` section with REPO-03, REPO-04, REPO-05 subsections.
- Any defect found is classified (trivial → fixed inline; substantial → logged + flagged for gap plan) and recorded.
</verification>

<success_criteria>
- REPO-03, REPO-04, REPO-05 verified end-to-end in the browser across bg and en.
- No unlogged defects; substantial defects (if any) flagged for a follow-up gap plan rather than rebuilt.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-agency-repositioning/08-03-SUMMARY.md`.
</output>
