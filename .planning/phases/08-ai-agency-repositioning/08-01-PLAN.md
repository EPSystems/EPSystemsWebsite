---
phase: 08-ai-agency-repositioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-ai-agency-repositioning/08-FINDINGS.md
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "pnpm build exits 0 with no TypeScript errors"
    - "Visiting / redirects to /bg/ (Bulgarian is the default locale)"
    - "Every required route is wired in src/App.tsx"
    - "All 5 service slugs, 3 industry slugs, and 2 locales are present in source as documented"
  artifacts:
    - path: src/App.tsx
      provides: "Route table with /:lang prefix and root redirect to /bg/"
      contains: 'Navigate to="/bg/"'
    - path: src/i18n/index.ts
      provides: "i18next config defaulting to bg"
      contains: "fallbackLng: 'bg'"
    - path: .planning/phases/08-ai-agency-repositioning/08-FINDINGS.md
      provides: "Defect log started for the phase (header + build/route inventory result)"
      min_lines: 5
  key_links:
    - from: src/App.tsx
      to: "/bg/"
      via: "root Navigate redirect"
      pattern: 'Navigate to="/bg/" replace'
    - from: src/i18n/index.ts
      to: "react-i18next"
      via: "initReactI18next with lng/fallbackLng bg"
      pattern: "fallbackLng: 'bg'"
---

<objective>
Establish the verification foundation for Phase 8: prove the shipped codebase compiles cleanly and that the entire route/source inventory required by REPO-01…REPO-09 actually exists and is wired. This plan is the Wave 1 gate — the two browser-verification plans (08-02, 08-03) depend on a green production build before they spin up a server.

This plan also verifies REPO-01 (Bulgarian-first defaulting) at the source + redirect level.

Purpose: A green build + complete route inventory de-risks all downstream browser checks. If the build is red, browser verification is meaningless.
Output: A confirmed-green build, a verified source inventory, and `.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md` initialized as the phase defect log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@package.json
@src/App.tsx

<interfaces>
<!-- Source of truth for routes and slugs. Executor uses these directly — no exploration needed. -->

Package scripts (from package.json):
- `pnpm dev` → vite (dev server, default http://localhost:5173)
- `pnpm build` → `tsc && vite build` (prebuild runs `node scripts/generate-sitemap.mjs`)
- `pnpm preview` → vite preview (serves the built dist/, default http://localhost:4173)

Routes (from src/App.tsx):
- `/` → `<Navigate to="/bg/" replace />`
- `/:lang` → HomePage
- `/:lang/services` → Services (hub)
- `/:lang/services/:slug` → ServicePage
- `/:lang/industries/:slug` → Industry
- `/:lang/contact` → Contact
- `/:lang/pricing` → Pricing
- `/:lang/blog` → Blog
- `/:lang/blog/:slug` → BlogPost
- `/:lang/privacy-policy` → PrivacyPolicy
- `/:lang/about` → About
- `*` → NotFoundPage
- NOTE: `/:lang/projects` is commented out (intentionally unrouted — PROJ-01 is a future requirement). Do NOT flag this as a defect.

Service slugs (from src/pages/ServicePage.tsx VALID_SLUGS): ai-websites, ai-automation, ai-agents, ai-seo, ai-ecommerce
Industry slugs (from src/pages/Industry.tsx VALID_SLUGS): insurance, ecommerce, fitness
Locales (from src/i18n/index.ts): bg (default + fallback), en

i18n config (src/i18n/index.ts): `lng: 'bg'`, `fallbackLng: 'bg'`. NOTE: there is NO i18next-browser-languagedetector and NO localStorage persistence — the URL `/:lang` segment (via useLanguageSync) is the source of truth for the active language. REPO-01's "returning-user preference persists" clause is therefore NOT implemented via a language detector; persistence happens only by URL/bookmark. Record this as a finding (not a build-blocking defect).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build gate</name>
  <read_first>package.json</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    Confirm dependencies are installed, then run the production build and capture the result.
    Steps (run from repo root D:\Claude\EPSystems):
    1. If `node_modules` is absent or the build fails on a missing module, run `pnpm install` first.
    2. Run `pnpm build`. This runs `prebuild` (generate-sitemap), then `tsc` (TypeScript typecheck), then `vite build`.
    3. Capture the exit code and the tail of stdout/stderr.

    Create `.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md` with this exact header structure:
    ```
    # Phase 08 — Verification Findings

    > Log defects here as they are found. Trivial defects (broken link, missing
    > translation key, wrong slug) are fixed inline in the same task. Substantial
    > defects (missing page, broken feature) are logged here and flagged for a
    > follow-up gap plan — do NOT silently rebuild.

    ## Build & Route Inventory (Plan 08-01)
    - Build (`pnpm build`): {PASS exit 0 | FAIL exit N — paste error}
    ```
    Append the build result. If the build FAILS with a TypeScript error, log the exact error here and STOP — a red build is a substantial defect; flag it for a gap plan and do not attempt to refactor source.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits with code 0.
    - No `error TS` lines appear in build output.
    - `dist/index.html` exists after the build.
    - `08-FINDINGS.md` exists and contains the line `- Build (\`pnpm build\`):` with a PASS/FAIL result recorded.
  </acceptance_criteria>
  <done>Production build is green (exit 0, no TS errors, dist/ produced) and the finding log records it. If red, the TS/build error is logged as a substantial defect and flagged for a gap plan.</done>
</task>

<task type="auto">
  <name>Task 2: Route + slug source inventory (REPO-01 source-level proof)</name>
  <read_first>src/App.tsx, src/i18n/index.ts, src/pages/ServicePage.tsx, src/pages/Industry.tsx</read_first>
  <files>.planning/phases/08-ai-agency-repositioning/08-FINDINGS.md</files>
  <action>
    Assert via grep/read that every route, slug, and locale required by REPO-01…REPO-09 exists in source. Do NOT start a browser here — this is static source verification only.

    Run these checks (grep against the listed files) and record each as PASS/FAIL in 08-FINDINGS.md under a `### Route inventory` subsection:
    1. Root redirect: `src/App.tsx` contains `Navigate to="/bg/" replace` (REPO-01 — / → /bg/).
    2. i18n default: `src/i18n/index.ts` contains `lng: 'bg'` AND `fallbackLng: 'bg'` (REPO-01 — BG-first).
    3. Every page route is present in `src/App.tsx`: `path="/:lang"`, `/:lang/services`, `/:lang/services/:slug`, `/:lang/industries/:slug`, `/:lang/contact`, `/:lang/pricing`, `/:lang/about`, `/:lang/privacy-policy`, `/:lang/blog`, `/:lang/blog/:slug`.
    4. Service slugs: `src/pages/ServicePage.tsx` VALID_SLUGS contains all of `ai-websites`, `ai-automation`, `ai-agents`, `ai-seo`, `ai-ecommerce`.
    5. Industry slugs: `src/pages/Industry.tsx` VALID_SLUGS contains all of `insurance`, `ecommerce`, `fitness`.
    6. Each page route maps to an imported component in `src/App.tsx` (HomePage, Services, ServicePage, Industry, Contact, Pricing, About, PrivacyPolicy, Blog, BlogPost).

    REPO-01 persistence note: confirm there is NO `i18next-browser-languagedetector` import in `src/i18n/index.ts` and NO `localStorage` language read. Record this as a finding: "REPO-01 persistence is URL-driven only (no language detector / localStorage). Not a defect — language persists via URL/bookmark." This is informational, not blocking.

    Trivial fix policy: if a route is wired to a non-existent component import, or a slug array is missing one documented slug, that is a substantial defect — log it and flag for a gap plan, do not invent the missing page.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('src/App.tsx','utf8');const i=fs.readFileSync('src/i18n/index.ts','utf8');const sp=fs.readFileSync('src/pages/ServicePage.tsx','utf8');const ind=fs.readFileSync('src/pages/Industry.tsx','utf8');const need=['/:lang','/:lang/services','/:lang/services/:slug','/:lang/industries/:slug','/:lang/contact','/:lang/pricing','/:lang/about','/:lang/privacy-policy','/:lang/blog','/:lang/blog/:slug'];const sv=['ai-websites','ai-automation','ai-agents','ai-seo','ai-ecommerce'];const id=['insurance','ecommerce','fitness'];const miss=[];if(!a.includes('Navigate to=\"/bg/\"'))miss.push('root redirect');if(!i.includes(\"fallbackLng: 'bg'\"))miss.push('fallbackLng bg');need.forEach(p=>{if(!a.includes('path=\"'+p+'\"'))miss.push('route '+p)});sv.forEach(s=>{if(!sp.includes(\"'\"+s+\"'\"))miss.push('service '+s)});id.forEach(s=>{if(!ind.includes(\"'\"+s+\"'\"))miss.push('industry '+s)});if(miss.length){console.error('MISSING:',miss.join(', '));process.exit(1)}console.log('INVENTORY OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The inventory script prints `INVENTORY OK` and exits 0 (all routes, 5 service slugs, 3 industry slugs, root redirect, and `fallbackLng: 'bg'` confirmed present).
    - `08-FINDINGS.md` contains a `### Route inventory` subsection with a PASS/FAIL line for each of the 6 checks.
    - The REPO-01 persistence note is recorded in `08-FINDINGS.md`.
  </acceptance_criteria>
  <done>Every required route, all 5 service slugs, all 3 industry slugs, the root→/bg/ redirect, and the bg i18n default are confirmed present in source. The REPO-01 persistence mechanism (URL-driven) is documented. Inventory result is in the finding log.</done>
</task>

</tasks>

<verification>
- `pnpm build` exits 0 with no `error TS` output and produces `dist/`.
- The inventory script prints `INVENTORY OK`.
- `08-FINDINGS.md` exists with the build result, the per-check route-inventory table, and the REPO-01 persistence note.
- No substantial defects found (or, if found, each is logged and flagged for a gap plan — not rebuilt here).
</verification>

<success_criteria>
- REPO-01 verified at source level: `/` → `/bg/` redirect present, i18n defaults+falls back to `bg`.
- Build gate is green, unblocking Wave 2 browser plans.
- Phase finding log initialized for downstream plans to append to.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-agency-repositioning/08-01-SUMMARY.md`.
</output>
