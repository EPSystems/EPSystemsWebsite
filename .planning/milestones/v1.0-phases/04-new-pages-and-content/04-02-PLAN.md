---
phase: 04-new-pages-and-content
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/App.tsx
  - src/pages/ServicePage.tsx
  - src/components/sections/ServiceHero.tsx
  - src/components/sections/ServiceFeatures.tsx
  - src/components/sections/ServiceProcess.tsx
  - src/components/sections/ServiceCTA.tsx
  - src/components/sections/Services.tsx
  - src/pages/HomePage.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-04

must_haves:
  truths:
    - "User can navigate to /en/services/seo and see a full service page with hero, features, process, and CTA sections"
    - "User can navigate to /bg/services/software and see the same page in Bulgarian"
    - "Invalid slugs like /en/services/invalid redirect to homepage"
    - "Service cards on homepage link to dedicated service pages (not scroll to detail sections)"
    - "Homepage no longer has inline ServiceDetail sections (shorter, cleaner page)"
    - "Navbar links work on both homepage (scroll) and service pages (navigate to homepage then scroll)"
    - "Footer service links navigate to dedicated service pages"
  artifacts:
    - path: "src/pages/ServicePage.tsx"
      provides: "Shared service page template using URL slug to load content"
      exports: ["ServicePage"]
    - path: "src/components/sections/ServiceHero.tsx"
      provides: "Hero section for service pages"
      exports: ["ServiceHero"]
    - path: "src/components/sections/ServiceFeatures.tsx"
      provides: "Features list section for service pages"
      exports: ["ServiceFeatures"]
    - path: "src/components/sections/ServiceProcess.tsx"
      provides: "Process/how-we-work section for service pages"
      exports: ["ServiceProcess"]
    - path: "src/components/sections/ServiceCTA.tsx"
      provides: "CTA section with unique button text per service"
      exports: ["ServiceCTA"]
    - path: "src/App.tsx"
      provides: "Route for /:lang/services/:slug"
      contains: "ServicePage"
  key_links:
    - from: "src/pages/ServicePage.tsx"
      to: "src/i18n/locales/en/common.json"
      via: "t() calls using servicePages.{slug}.* keys"
      pattern: "servicePages\\..*\\."
    - from: "src/components/sections/Services.tsx"
      to: "src/pages/ServicePage.tsx"
      via: "Link to /{lang}/services/{slug} for 4 services with dedicated pages"
      pattern: "services/"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/pages/HomePage.tsx"
      via: "navigate to homepage + scrollToSection when on service page"
      pattern: "isHomePage"
---

<objective>
Build the service page template, 4 section components, register routes, and update all navigation to work seamlessly between homepage and service pages.

Purpose: Users can visit dedicated service pages at unique URLs with full bilingual content, and navigation between pages and homepage is seamless.
Output: ServicePage.tsx with 4 sub-components, updated App.tsx route, updated Services.tsx/Navbar.tsx/Footer.tsx/HomePage.tsx for proper navigation.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-new-pages-and-content/04-CONTEXT.md
@.planning/phases/04-new-pages-and-content/04-RESEARCH.md
@.planning/phases/04-new-pages-and-content/04-01-SUMMARY.md

@src/App.tsx
@src/pages/HomePage.tsx
@src/components/sections/Services.tsx
@src/components/sections/ServiceDetail.tsx
@src/components/layout/Navbar.tsx
@src/components/layout/Footer.tsx
@src/hooks/useLanguageSync.ts
@src/hooks/usePageMeta.ts
@src/components/ui/AnimatedSection.tsx

<interfaces>
From src/hooks/useLanguageSync.ts:
```typescript
export function useLanguageSync(): void
// Syncs URL :lang param to i18next language. Must be called in every page component.
```

From src/hooks/usePageMeta.ts:

```typescript
export function usePageMeta(titleKey: string, descriptionKey: string): void;
// Sets document.title and meta description from translation keys.
```

From src/utils/scroll.ts:

```typescript
export function scrollToSection(sectionId: string): void;
// Wraps getElementById + scrollIntoView({ behavior: 'smooth' })
```

From src/data/services.ts (after Plan 01 rename):

```typescript
// Service IDs: 'seo', 'software', 'ecommerce', 'ai', 'landing', 'support'
// 4 services get dedicated pages: seo, ecommerce, ai, software
// 2 services stay homepage-only: landing, support
```

From translation JSON (after Plan 01):

```
servicePages.{slug}.meta.title / description
servicePages.{slug}.hero.badge / heading / subheading
servicePages.{slug}.features.heading / items.{N}.title / items.{N}.description
servicePages.{slug}.process.heading / steps.{N}.title / steps.{N}.description
servicePages.{slug}.cta.heading / description / button
servicePages.backToServices
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicePage template and 4 section components</name>
  <files>src/pages/ServicePage.tsx, src/components/sections/ServiceHero.tsx, src/components/sections/ServiceFeatures.tsx, src/components/sections/ServiceProcess.tsx, src/components/sections/ServiceCTA.tsx</files>
  <action>
1. Create `src/components/sections/ServiceHero.tsx`:
   - Props: `{ slug: string }`
   - Uses `useTranslation()` to read `servicePages.{slug}.hero.*` keys
   - Renders: badge (styled like existing badges - inline-block, bg-[#B9FF66], border-2, rounded-lg, -rotate-2), heading (text-5xl lg:text-6xl font-black tracking-tighter), subheading (text-xl text-zinc-600 font-medium, max-w-2xl)
   - Wrapped in AnimatedSection
   - Section has `max-w-7xl mx-auto px-6 pt-32 pb-16` (pt-32 for navbar clearance)
   - Include a "back to services" link at the top: `<Link to={/${lang}/} ...>` with arrow icon, using `t('servicePages.backToServices')` text. Style: text-lg font-bold, hover:text-[#88cc33], with ArrowLeft icon.

2. Create `src/components/sections/ServiceFeatures.tsx`:
   - Props: `{ slug: string }`
   - Reads `servicePages.{slug}.features.*` keys
   - Renders heading + grid of 4 feature cards (2 cols on lg, 1 on mobile)
   - Each card: bg-white p-8 rounded-[30px] border-4 border-black brutalist-shadow-static, with feature title (text-2xl font-black) and description (text-lg text-zinc-600)
   - Alternate card accent colors: odd cards get a lime top border accent (border-t-[#B9FF66]), even cards get black
   - Use AnimatedSection and motion.div with stagger animation (same pattern as Services.tsx cards)
   - Iterate items 0-3 using array index keys

3. Create `src/components/sections/ServiceProcess.tsx`:
   - Props: `{ slug: string }`
   - Reads `servicePages.{slug}.process.*` keys
   - Renders heading + horizontal numbered steps (4 steps)
   - Each step: large step number (text-6xl font-black text-[#B9FF66]), title (text-xl font-black), description (text-zinc-600)
   - Layout: grid with 4 columns on lg, 2 on md, 1 on mobile
   - Connecting line between steps on desktop (use border-t-4 border-dashed border-black on the row, or a pseudo-element approach)
   - Section background: bg-zinc-100 with rounded-[50px] border-4 border-black (similar to the boxed ServiceDetail variant)
   - Wrapped in AnimatedSection

4. Create `src/components/sections/ServiceCTA.tsx`:
   - Props: `{ slug: string }`
   - Reads `servicePages.{slug}.cta.*` keys
   - Renders: heading (text-4xl font-black), description, and a prominent CTA button
   - CTA button: `<a href="mailto:engineering@epsystems.org">` with the service-specific button text from `t('servicePages.{slug}.cta.button')`
   - Style: bg-[#B9FF66] border-4 border-black rounded-xl px-10 py-5 text-xl font-black brutalist-shadow
   - Center-aligned section with max-w-3xl
   - Wrapped in AnimatedSection

5. Create `src/pages/ServicePage.tsx`:
   - Shared template that renders all 4 service pages
   - Uses `useParams()` to get `lang` and `slug`
   - Calls `useLanguageSync()` at top (per Research pitfall 4)
   - Validates slug against `VALID_SLUGS = ['seo', 'ecommerce', 'ai', 'software'] as const`
   - If invalid slug, `<Navigate to={/${lang || 'en'}/} replace />`
   - Calls `usePageMeta('servicePages.${slug}.meta.title', 'servicePages.${slug}.meta.description')`
   - Renders: `<Navbar />`, `<ServiceHero slug={slug} />`, `<ServiceFeatures slug={slug} />`, `<ServiceProcess slug={slug} />`, `<ServiceCTA slug={slug} />`, `<Footer />`
   - All sections use `max-w-7xl mx-auto px-6 py-16` or `py-24` for consistent spacing

All components must use the existing Brutalist design language: border-4 border-black, rounded-[30px], brutalist-shadow classes, font-black, tracking-tighter. Match the aesthetic of existing components like Services.tsx and ServiceDetail.tsx.
</action>
<verify>
Run `npx vite build` to confirm all new components compile. Verify all 5 new files exist and export their components.
</verify>
<done>ServicePage.tsx exists with slug validation, language sync, and page meta. All 4 section components render using servicePages.{slug}.\* translation keys. Brutalist design language is consistent with existing site aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Register route and update all navigation (homepage, navbar, footer)</name>
  <files>src/App.tsx, src/pages/HomePage.tsx, src/components/sections/Services.tsx, src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx</files>
  <action>
1. In `src/App.tsx`:
   - Import `ServicePage` from `./pages/ServicePage`
   - Add route `<Route path="/:lang/services/:slug" element={<ServicePage />} />` between the `/:lang` and `*` routes

2. In `src/pages/HomePage.tsx`:
   - Remove the entire `detailConfig` array and the `detailSections` mapping logic
   - Remove the `{detailSections.map(...)}` rendering block
   - Remove the `ServiceDetail` import and the `Search, ShoppingCart, Bot` icon imports (no longer needed)
   - The homepage now goes: Navbar -> Hero -> Marquee -> Services -> CaseStudies -> CTA -> Footer
   - NOTE: CaseStudies was already before Footer but after the ServiceDetail sections. With those removed, CaseStudies now follows Services directly. Keep CTA between CaseStudies and Footer (reorder if needed: Services -> CaseStudies -> CTA -> Footer, matching the existing order minus ServiceDetail sections)

3. In `src/components/sections/Services.tsx`:
   - Import `Link` and `useParams` from `react-router-dom`
   - Define a constant mapping service IDs to URL slugs for the 4 services with pages:
     ```typescript
     const SERVICE_SLUGS: Record<string, string> = {
       seo: "seo",
       ecommerce: "ecommerce",
       ai: "ai",
       software: "software",
     };
     ```
   - Get `lang` from `useParams()`
   - In the card render, replace the `scrollToSection` button with a `<Link>` for services that have slugs:
     - If `SERVICE_SLUGS[service.id]` exists: render `<Link to={/${lang}/services/${SERVICE_SLUGS[service.id]}}>` with "Learn more" text
     - Otherwise: no "Learn more" link (same as current behavior for services without detailHeadline)
   - Remove the `scrollToSection` import if no longer used in this file
   - Keep the existing `hasDetail` check but change the logic: `const hasPage = Boolean(SERVICE_SLUGS[service.id])`

4. In `src/components/layout/Navbar.tsx`:
   - Import `useLocation` and `useNavigate` from react-router-dom (already has `useParams` and `Link`)
   - Add logic to detect if user is on homepage: `const location = useLocation(); const isHomePage = location.pathname === '/${lang}/' || location.pathname === '/${lang}'`
   - Update `handleScroll` to navigate home first if not on homepage:
     ```typescript
     const navigate = useNavigate();
     const handleScroll = (sectionId: string) => {
       if (isHomePage) {
         scrollToSection(sectionId);
       } else {
         navigate(`/${lang}/`);
         setTimeout(() => scrollToSection(sectionId), 100);
       }
     };
     ```
   - Same for `handleMobileScroll` - close mobile menu, then use the same logic
   - This ensures clicking "Services" or "SEO" in navbar while on a service page navigates back to homepage and scrolls to the right section

5. In `src/components/layout/Footer.tsx`:
   - Change the 3 service links in the "Services" column from `scrollToSection` buttons to `<Link>` elements:
     - SEO: `<Link to={/${lang}/services/seo}>`
     - E-Commerce: `<Link to={/${lang}/services/ecommerce}>`
     - AI & Automation: `<Link to={/${lang}/services/ai}>`
   - Keep the same styling (text-lg text-white hover:text-[#B9FF66] transition-colors font-bold tracking-tight)
   - The "Services" link in the Sitemap column stays as `scrollToSection('services')` (or use the same navigate-then-scroll pattern since Footer is on all pages now)
   - Actually, since Footer appears on service pages too, update the Sitemap "Services" button to also use navigate-then-scroll: import `useLocation`, `useNavigate`, check `isHomePage`, same pattern as Navbar
     </action>
     <verify>
     Run `npx vite build` to confirm no build errors. Run `npx vite preview` and manually verify:
6. Visit /en/services/seo - full page renders with hero, features, process, CTA
7. Visit /en/services/invalid - redirects to /en/
8. Click "Learn more" on SEO card from homepage - navigates to /en/services/seo
9. Click "Services" in navbar while on service page - returns to homepage services section
10. Footer "SEO" link navigates to /en/services/seo
    </verify>
    <done>Service pages accessible at /:lang/services/:slug for all 4 services. Homepage no longer shows inline ServiceDetail sections. Service cards link to dedicated pages. Navbar scroll works on both homepage and service pages. Footer links to service pages directly.</done>
    </task>

</tasks>

<verification>
1. `npx vite build` succeeds
2. All 4 service URLs render correctly: /en/services/seo, /en/services/ecommerce, /en/services/ai, /en/services/software
3. Same 4 URLs work in Bulgarian: /bg/services/seo, etc.
4. Invalid slug /en/services/xyz redirects to /en/
5. Service cards on homepage link to dedicated pages (not scroll)
6. Navbar "Services"/"SEO"/"E-Commerce" buttons work on both homepage (scroll) and service pages (navigate+scroll)
7. Footer service links are `<Link>` elements to service pages
8. Homepage is shorter - no inline ServiceDetail sections
9. Page title and meta description update per service page and language
</verification>

<success_criteria>

- 4 service pages render at unique URLs with full content in both languages
- Navigation between homepage and service pages is seamless in both directions
- Homepage ServiceDetail sections are removed, replaced by service card links
- Consistent Brutalist design across all new pages
  </success_criteria>

<output>
After completion, create `.planning/phases/04-new-pages-and-content/04-02-SUMMARY.md`
</output>
