---
phase: 04-new-pages-and-content
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/sections/Team.tsx
  - src/pages/HomePage.tsx
autonomous: false
requirements:
  - CONT-01

must_haves:
  truths:
    - "User sees a team section on the homepage with 3 member cards showing name, role, and placeholder avatar"
    - "Team section appears after CaseStudies and before CTA on the homepage"
    - "Team section displays in Bulgarian when browsing /bg/"
  artifacts:
    - path: "src/components/sections/Team.tsx"
      provides: "Team member grid with Brutalist card design"
      exports: ["Team"]
      min_lines: 30
  key_links:
    - from: "src/components/sections/Team.tsx"
      to: "src/i18n/locales/en/common.json"
      via: "t() calls using team.* keys"
      pattern: "team\\."
    - from: "src/pages/HomePage.tsx"
      to: "src/components/sections/Team.tsx"
      via: "import and render between CaseStudies and CTA"
      pattern: "<Team"
---

<objective>
Add a team members section to the homepage showing 3 team members with name, role, and placeholder avatar in a Brutalist card layout.

Purpose: Present E&P Systems as a real team of people, building trust with potential clients. Positioned after CaseStudies to show "here are the people who built those projects."
Output: Team.tsx component integrated into HomePage between CaseStudies and CTA.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-new-pages-and-content/04-CONTEXT.md
@.planning/phases/04-new-pages-and-content/04-02-SUMMARY.md

@src/pages/HomePage.tsx
@src/data/team.ts
@src/i18n/locales/en/common.json

<interfaces>
From src/data/team.ts (created in Plan 01):
```typescript
export interface TeamMember {
  id: string
}

export const teamMembers: TeamMember[] = [
{ id: 'member1' },
{ id: 'member2' },
{ id: 'member3' },
]

```

From translation JSON (created in Plan 01):
```

team.badge -> "Our Team" / "Нашият екип"
team.heading -> "The People Behind E&P Systems" / "Хората зад E&P Systems"
team.members.{id}.name -> member name
team.members.{id}.role -> member role

````

From existing components:
```typescript
// AnimatedSection wraps children with framer-motion fade-in
import { AnimatedSection } from '../ui/AnimatedSection'

// Lucide icons available for placeholder avatar
import { User } from 'lucide-react'
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Team section component and add to HomePage</name>
  <files>src/components/sections/Team.tsx, src/pages/HomePage.tsx</files>
  <action>
1. Create `src/components/sections/Team.tsx`:
   - Import `useTranslation` from react-i18next
   - Import `teamMembers` from `../../data/team`
   - Import `User` icon from lucide-react for placeholder avatars
   - Import `AnimatedSection` from `../ui/AnimatedSection`
   - Import `motion` from framer-motion for card stagger animation

Structure:

- Section wrapper: `<section className="max-w-7xl mx-auto px-6 py-24">` (no id needed since not a nav target)
- Badge: same style as other sections - `inline-block bg-[#B9FF66] text-black font-bold px-4 py-2 border-2 border-black rounded-lg mb-6 transform -rotate-2`
- Badge text: `t('team.badge')`
- Heading: `text-4xl lg:text-5xl font-bold tracking-tighter leading-tight mb-16`, text: `t('team.heading')`
- Cards grid: `grid md:grid-cols-3 gap-8`
- Each card (motion.div with stagger):
  - `bg-white p-8 rounded-[30px] border-4 border-black brutalist-shadow-static text-center`
  - Placeholder avatar: `<div className="w-32 h-32 mx-auto mb-6 bg-[#B9FF66] border-4 border-black rounded-full flex items-center justify-center"><User size={48} className="text-black" /></div>`
  - Name: `<h3 className="text-2xl font-black tracking-tighter">{t('team.members.${member.id}.name')}</h3>`
  - Role: `<p className="text-lg text-zinc-600 font-bold mt-2">{t('team.members.${member.id}.role')}</p>`
- Card animation: same pattern as Services.tsx - `initial={{ y: 80, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true, margin: '-20%' }} transition={{ duration: 0.7, ease: 'easeOut', delay: i * 0.15 }}`

No bio text, no social links per user decision. CSS placeholder avatar (not img tag) per Research pitfall 6.

2. In `src/pages/HomePage.tsx`:
   - Import `Team` from `../components/sections/Team`
   - Add `<Team />` between `<CaseStudies />` and `<CTA />` in the JSX
   - The order should be: Navbar -> Hero -> Marquee -> Services -> CaseStudies -> Team -> CTA -> Footer
   - Per user decision: "Positioned before the CTA section (after Case Studies)"
     </action>
     <verify>
     Run `npx vite build` to confirm no build errors. Run `npx vite preview` and verify:
1. Homepage shows team section between Case Studies and CTA
1. Three cards visible with placeholder avatars, names, and roles
1. Switch to /bg/ - team section shows Bulgarian text
   </verify>
   <done>Team section renders on homepage with 3 member cards (placeholder avatar, name, role). Section is positioned after CaseStudies and before CTA. Content is bilingual - displays in Bulgarian on /bg/ route.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 4 deliverables</name>
  <action>User verifies all Phase 4 work: service pages, team section, rename, navigation, and bilingual content quality.</action>
  <what-built>Complete Phase 4: 4 service detail pages (SEO, E-Commerce, AI & Automation, Custom Software) at unique URLs, team section on homepage, "Custom Websites" renamed to "Custom Software" site-wide, all content bilingual.</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or use preview URL if already running)
    2. Visit http://localhost:5173/en/ - homepage should show:
       - Service cards with "Learn more" links (no inline detail sections)
       - Team section with 3 members after Case Studies
       - "Custom Software" (not "Custom Websites") on the software service card
    3. Click "Learn more" on the SEO card - should navigate to /en/services/seo with full content (Hero, Features, Process, CTA)
    4. Check all 4 service pages: /en/services/seo, /en/services/ecommerce, /en/services/ai, /en/services/software
    5. Switch to Bulgarian: /bg/services/seo - all content in Bulgarian
    6. On a service page, click "Services" in navbar - should return to homepage services section
    7. Check Footer service links point to service pages
    8. Review content quality - professional, accurate, no placeholder text
    9. Review Bulgarian translations - natural, not machine-translated feel
  </how-to-verify>
  <verify>User approves all deliverables</verify>
  <done>User has verified and approved all Phase 4 deliverables</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe any content/design/navigation issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. Team section visible on homepage between CaseStudies and CTA
2. 3 member cards with name + role + placeholder avatar
3. Bilingual team content (EN/BG)
4. Full end-to-end Phase 4 verification via human checkpoint
</verification>

<success_criteria>

- Team section renders correctly on homepage with Brutalist design
- All Phase 4 deliverables verified by user: service pages, team section, rename, bilingual content
- User approves content quality and design
  </success_criteria>

<output>
After completion, create `.planning/phases/04-new-pages-and-content/04-03-SUMMARY.md`
</output>
