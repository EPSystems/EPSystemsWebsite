---
phase: 03-full-content-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/common.json
  - src/i18n/locales/bg/common.json
  - src/components/sections/Hero.tsx
  - src/components/sections/Marquee.tsx
  - src/components/sections/Services.tsx
  - src/components/sections/CTA.tsx
  - src/data/services.ts
autonomous: true
requirements:
  - BILN-04
  - BILN-05

must_haves:
  truths:
    - "User browsing in Bulgarian sees Hero section fully in Bulgarian (badge, heading, subheading, buttons)"
    - "User browsing in Bulgarian sees all 6 service cards with Bulgarian titles, descriptions, and features"
    - "User browsing in Bulgarian sees Marquee ticker in Bulgarian"
    - "User browsing in Bulgarian sees CTA section fully in Bulgarian"
    - "User browsing in Bulgarian sees Services section header in Bulgarian"
  artifacts:
    - path: "src/i18n/locales/en/common.json"
      provides: "All English translation keys for hero, services, marquee, cta sections"
      contains: "hero"
    - path: "src/i18n/locales/bg/common.json"
      provides: "All Bulgarian translation keys matching EN structure"
      contains: "hero"
    - path: "src/data/services.ts"
      provides: "Service data using i18n translation keys instead of hardcoded strings"
      contains: "useTranslation"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/i18n/locales/*/common.json"
      via: "useTranslation hook t() calls"
      pattern: "t\\('hero\\."
    - from: "src/data/services.ts"
      to: "src/i18n/locales/*/common.json"
      via: "translation keys for service data"
      pattern: "services\\."
---

<objective>
Expand translation JSON files with all homepage content keys and wire the primary homepage sections (Hero, Marquee, Services grid, CTA) to use i18n t() calls instead of hardcoded English strings. Also translate services.ts data through i18n.

Purpose: This is the bulk translation work — converting the largest sections from hardcoded English to bilingual content via the existing i18n system.
Output: EN/BG JSON files with all section keys, 5 components converted to use t() calls.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/i18n/index.ts
@src/i18n/locales/en/common.json
@src/i18n/locales/bg/common.json
@src/components/sections/Hero.tsx
@src/components/sections/Marquee.tsx
@src/components/sections/Services.tsx
@src/components/sections/CTA.tsx
@src/data/services.ts
@src/pages/HomePage.tsx

<interfaces>
<!-- Existing i18n setup from Phase 2 -->

From src/i18n/index.ts:
```typescript
// i18next configured with bundled JSON, defaultNS: 'common'
// Resources: { en: { common: en }, bg: { common: bg } }
```

From src/hooks/useLanguageSync.ts:
```typescript
// Syncs i18n language with URL :lang param
// Sets document.documentElement.lang
```

Usage pattern (from Navbar/Footer):
```typescript
import { useTranslation } from 'react-i18next'
const { t } = useTranslation()
// t('nav.services') -> "Services" or "Услуги"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand EN/BG translation JSON files with all section keys</name>
  <files>src/i18n/locales/en/common.json, src/i18n/locales/bg/common.json</files>
  <action>
Add the following key sections to BOTH en/common.json and bg/common.json, preserving existing nav/footer keys:

**hero section:**
- hero.badge: "We build digital solutions that work" / "Изграждаме дигитални решения, които работят"
- hero.heading1: "Elevate Your" / "Издигнете Вашето"
- hero.heading2: "Digital Presence" / "Дигитално Присъствие"
- hero.subheading: (current EN paragraph) / BG equivalent — natural, not mechanical
- hero.cta.contact: "Get in Touch" / "Свържете се с нас"
- hero.cta.work: "See Our Work" / "Вижте нашите проекти"
- hero.dashboard.title: "Project Growth" / "Растеж на проекта"

**services section header:**
- services.badge: "Our Services" / "Нашите услуги"
- services.heading: (current EN heading) / BG equivalent
- services.learnMore: "Learn more" / "Научете повече"

**services data (all 6 services):**
- services.seo.title / services.seo.titleBreak / services.seo.description / services.seo.features (array as features.0, features.1, features.2) / services.seo.detailHeadline / services.seo.detailDescription / services.seo.ctaText
- services.websites.title / titleBreak / description / features.0-2
- services.ecommerce.title / titleBreak / description / features.0-2 / detailHeadline / detailDescription / ctaText
  - IMPORTANT: Use "Онлайн магазин" not "Е-Комерс" per user decision
- services.ai.title / titleBreak / description / features.0-2 / detailHeadline / detailDescription / ctaText
- services.landing.title / titleBreak / description / features.0-2
- services.support.title / titleBreak / description / features.0-2

**marquee:**
- marquee.items (array as marquee.0 through marquee.5): "Web Development", "E-Commerce", "SEO", "AI Automation", "Landing Pages", "Custom Apps" / Bulgarian equivalents
  - Use "Онлайн магазин" for E-Commerce marquee item in BG

**cta section:**
- cta.heading: "Let's build something great" / "Нека изградим нещо страхотно"
- cta.description: (current EN text) / BG equivalent
- cta.button: "Get your free proposal" / "Получете безплатна оферта"

Bulgarian translations must sound natural and professional, not literal/mechanical. This is a software agency website — the tone should be confident and approachable.
  </action>
  <verify>
    <automated>node -e "const en = require('./src/i18n/locales/en/common.json'); const bg = require('./src/i18n/locales/bg/common.json'); const keys = ['hero.badge','services.badge','services.seo.title','marquee.0','cta.heading']; const ok = keys.every(k => { const parts = k.split('.'); let e=en,b=bg; for(const p of parts){e=e[p];b=b[p]} return e && b }); console.log(ok ? 'PASS: all key sections present' : 'FAIL: missing keys'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>Both JSON files contain hero, services (all 6), marquee, cta sections with natural Bulgarian translations. Existing nav/footer keys preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert services.ts to use i18n translation keys</name>
  <files>src/data/services.ts, src/components/sections/Services.tsx, src/pages/HomePage.tsx</files>
  <action>
The services.ts file currently exports a static array with hardcoded English strings. Convert it to work with i18n:

**Approach:** Create a `useServices()` hook (in src/data/services.ts or a new file src/hooks/useServices.ts) that uses `useTranslation()` and returns the services array with translated strings. The hook maps over a base array (keeping id, icon, variant) and fills in title, titleBreak, description, features, detailHeadline, detailDescription, ctaText from t() calls.

```typescript
// src/hooks/useServices.ts
import { useTranslation } from 'react-i18next'
import { services as serviceBase } from '../data/services'

export function useServices() {
  const { t } = useTranslation()
  return serviceBase.map(s => ({
    ...s,
    title: t(`services.${s.id}.title`),
    titleBreak: t(`services.${s.id}.titleBreak`),
    description: t(`services.${s.id}.description`),
    features: [0, 1, 2].map(i => t(`services.${s.id}.features.${i}`)),
    ...(s.detailHeadline ? {
      detailHeadline: t(`services.${s.id}.detailHeadline`),
      detailDescription: t(`services.${s.id}.detailDescription`),
      ctaText: t(`services.${s.id}.ctaText`),
    } : {}),
  }))
}
```

Update src/data/services.ts: Keep the static array but remove the English text strings from it — keep only id, icon, variant (and a flag for whether it has detail). Or simpler: keep all English strings as-is in services.ts (they serve as fallback) and just override via the hook.

Actually, simplest approach: Keep services.ts unchanged (it's the structural definition). Create useServices hook that overlays translations. Update Services.tsx to call `useServices()` instead of importing `services` directly. Update HomePage.tsx detailSections to use the translated services from useServices().

In Services.tsx:
- Import useServices instead of services from data
- Add `const { t } = useTranslation()` for section header text
- Replace "Our Services" badge with `t('services.badge')`
- Replace heading text with `t('services.heading')`
- Replace "Learn more" with `t('services.learnMore')`
- Use `useServices()` for the services.map()

In HomePage.tsx:
- Import and use `useServices()` to get translated services for detailSections
- The detailSections labels should also use translation keys
  </action>
  <verify>
    <automated>cd D:/Claude/EPSystems && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Services grid renders translated titles/descriptions/features. HomePage passes translated service data to ServiceDetail sections. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Hero, Marquee, and CTA components to use t() calls</name>
  <files>src/components/sections/Hero.tsx, src/components/sections/Marquee.tsx, src/components/sections/CTA.tsx</files>
  <action>
**Hero.tsx:**
- Add `import { useTranslation } from 'react-i18next'`
- Add `const { t } = useTranslation()` inside component
- Replace badge text with `t('hero.badge')`
- Replace "Elevate Your" with `t('hero.heading1')`
- Replace "Digital Presence" with `t('hero.heading2')`
- Replace subheading paragraph with `t('hero.subheading')`
- Replace "Get in Touch" with `t('hero.cta.contact')`
- Replace "See Our Work" with `t('hero.cta.work')`
- Replace "Project Growth" with `t('hero.dashboard.title')`
- Keep "+428%" as-is (numeric, language-independent)

**Marquee.tsx:**
- Add `import { useTranslation } from 'react-i18next'`
- Add `const { t } = useTranslation()` inside component
- Replace static items array with dynamic: `const items = [0,1,2,3,4,5].map(i => t('marquee.' + i))`
- This replaces the hardcoded English array with translated strings

**CTA.tsx:**
- Add `import { useTranslation } from 'react-i18next'`
- Add `const { t } = useTranslation()` inside component
- Replace heading with `t('cta.heading')`
- Replace paragraph with `t('cta.description')`
- Replace button text with `t('cta.button')`
  </action>
  <verify>
    <automated>cd D:/Claude/EPSystems && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Hero, Marquee, and CTA sections all render translated content based on current i18n language. No hardcoded English strings remain in these 3 files (except className values and technical attributes).</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Visual check: run `npm run dev`, visit /bg/ — Hero, Services grid, Marquee, CTA all display Bulgarian text with no English fragments
</verification>

<success_criteria>
- EN/BG JSON files contain all translation keys for hero, services (6), marquee, cta sections
- Hero, Marquee, CTA, Services components use t() calls — zero hardcoded display strings
- services.ts data is accessible via useServices() hook with translated content
- TypeScript compiles, build succeeds
- Bulgarian text uses "Онлайн магазин" not "Е-Комерс"
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-content-translation/03-01-SUMMARY.md`
</output>
